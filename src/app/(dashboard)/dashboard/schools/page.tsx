import { SchoolStatus } from "@prisma/client";
import { Search } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { requireAdmin } from "@/features/auth/server/auth";
import { CreateSchoolForm, SchoolStatusButton, UpdateSchoolForm } from "@/features/schools/components/school-forms";
import { listSchools } from "@/features/schools/queries/list-schools";

const first = (value: string | string[] | undefined) => Array.isArray(value) ? value[0] : value;
const date = new Intl.DateTimeFormat("tr-TR", { dateStyle: "medium" });
export default async function SchoolsPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) {
  await requireAdmin(); const params = await searchParams; const filters = { name: first(params.name), status: first(params.status) }; const result = await listSchools({ ...filters, page: Number(first(params.page)) });
  const href = (page: number) => { const query = new URLSearchParams(); if (filters.name) query.set("name", filters.name); if (filters.status) query.set("status", filters.status); if (page > 1) query.set("page", String(page)); return `/dashboard/schools${query.size ? `?${query}` : ""}`; };
  return <main className="mx-auto w-full max-w-7xl px-4 py-8 sm:px-6"><h1 className="text-3xl font-semibold">Okullar</h1><p className="mt-2 text-muted-foreground">Kayıtlarda kullanılacak okul adlarını merkezi olarak yönetin.</p><div className="mt-8 grid items-start gap-6 xl:grid-cols-[22rem_1fr]"><Card className="rounded-lg shadow-none"><CardHeader><CardTitle>Yeni Okul</CardTitle></CardHeader><CardContent><CreateSchoolForm /></CardContent></Card><Card className="gap-0 overflow-hidden rounded-lg py-0 shadow-none"><form method="get" className="grid gap-4 border-b p-5 sm:grid-cols-[1fr_12rem_auto] sm:items-end"><div className="grid gap-2"><Label htmlFor="school-search">Okul Adı</Label><Input id="school-search" name="name" defaultValue={filters.name} /></div><div className="grid gap-2"><Label htmlFor="school-status">Durum</Label><select id="school-status" name="status" defaultValue={filters.status ?? ""} className="h-9 rounded-md border bg-background px-3 text-sm"><option value="">Tümü</option><option value="ACTIVE">Aktif</option><option value="INACTIVE">Pasif</option></select></div><div className="flex gap-2"><Button><Search />Ara</Button>{result.hasFilters ? <Button asChild variant="outline"><Link href="/dashboard/schools">Temizle</Link></Button> : null}</div></form>{result.records.length ? <div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead className="pl-5">Okul</TableHead><TableHead>Durum</TableHead><TableHead>VR</TableHead><TableHead>QR</TableHead><TableHead>Oluşturulma</TableHead><TableHead>Güncellenme</TableHead><TableHead className="pr-5">İşlem</TableHead></TableRow></TableHeader><TableBody>{result.records.map((school) => <TableRow key={school.id}><TableCell className="pl-5"><UpdateSchoolForm id={school.id} name={school.name} /></TableCell><TableCell><Badge variant={school.status === SchoolStatus.ACTIVE ? "secondary" : "outline"}>{school.status === SchoolStatus.ACTIVE ? "Aktif" : "Pasif"}</Badge></TableCell><TableCell>{school._count.vrRecords}</TableCell><TableCell>{school._count.qrRegistrations}</TableCell><TableCell>{date.format(school.createdAt)}</TableCell><TableCell>{date.format(school.updatedAt)}</TableCell><TableCell className="pr-5"><SchoolStatusButton id={school.id} status={school.status} /></TableCell></TableRow>)}</TableBody></Table></div> : <div className="p-12 text-center">{result.hasFilters ? "Filtrelerle eşleşen okul bulunamadı." : "Henüz okul eklenmedi."}</div>}<div className="flex justify-between border-t p-5 text-sm"><span>Toplam {result.total} · Sayfa {result.page}/{result.pageCount}</span><div className="flex gap-2"><Button size="sm" variant="outline" asChild={result.page > 1} disabled={result.page <= 1}>{result.page > 1 ? <Link href={href(result.page - 1)}>Önceki</Link> : <span>Önceki</span>}</Button><Button size="sm" variant="outline" asChild={result.page < result.pageCount} disabled={result.page >= result.pageCount}>{result.page < result.pageCount ? <Link href={href(result.page + 1)}>Sonraki</Link> : <span>Sonraki</span>}</Button></div></div></Card></div></main>;
}
