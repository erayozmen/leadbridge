import { Inbox } from "lucide-react";
import type { ComponentProps, ReactNode } from "react";
import { cn } from "@/lib/utils";
export function FilterToolbar({ className, ...props }: ComponentProps<"form">) { return <form className={cn("grid gap-4 border-b border-border/70 bg-muted/15 p-4 sm:p-5 [&_label]:text-xs [&_label]:font-semibold [&_label]:text-muted-foreground", className)} {...props} />; }
export function DataEmptyState({ title, description, icon: Icon = Inbox, className }: { title: ReactNode; description?: ReactNode; icon?: typeof Inbox; className?: string }) { return <div className={cn("m-4 grid min-h-44 place-items-center rounded-lg border border-dashed border-border bg-muted/15 px-6 py-10 text-center sm:m-5", className)}><div><span className="mx-auto grid size-11 place-items-center rounded-full border bg-background text-muted-foreground"><Icon className="size-5" aria-hidden="true"/></span><p className="mt-4 text-sm font-semibold">{title}</p>{description ? <p className="mx-auto mt-1 max-w-md text-sm leading-6 text-muted-foreground">{description}</p> : null}</div></div>; }
export function DataPagination({ summary, children, className }: { summary: ReactNode; children: ReactNode; className?: string }) { return <div className={cn("flex flex-col gap-3 border-t border-border/70 bg-muted/10 px-4 py-4 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:px-5", className)}><span className="leading-5">{summary}</span><div className="flex max-w-full flex-wrap gap-2">{children}</div></div>; }
